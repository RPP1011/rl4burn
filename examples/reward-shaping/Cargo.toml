[package]
name = "reward-shaping"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rl4burn = { path = "../../rl4burn" }
burn = { version = "0.20", features = ["ndarray", "autodiff"] }
rand = "0.10"
